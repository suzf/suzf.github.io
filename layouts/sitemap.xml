{{- /* Sitemap.

       Two changes from the theme's version:

       - Taxonomy and term pages are excluded. They hold no content of their
         own and outnumber the posts 60 to 26; they are noindex now (see
         _default/baseof.html), so listing them here would contradict that and
         spend crawl budget on pages we have asked Google not to index.

       - hreflang uses the configured language code (zh-CN) rather than Hugo's
         internal lang key (zh-cn), so the sitemap agrees with what the pages
         themselves emit and with the main suzf.net site. */ -}}
{{- $pages := where .Data.Pages "Section" "!=" "gallery" -}}
{{- $pages = where $pages "Kind" "not in" (slice "taxonomy" "term") -}}
<urlset
    xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
    xmlns:xhtml="http://www.w3.org/1999/xhtml"
    >
    {{- range $pages -}}
        <url>
            <loc>
                {{- .Permalink -}}
            </loc>
            {{- if not .Lastmod.IsZero -}}
                <lastmod>
                    {{- .Lastmod.Format "2006-01-02T15:04:05-07:00" -}}
                </lastmod>
            {{- end -}}
            {{- with .Sitemap.ChangeFreq -}}
                <changefreq>
                    {{- . -}}
                </changefreq>
            {{- end -}}

            {{- if ge .Sitemap.Priority 0.0 -}}
                {{- $weeks := div (sub now.Unix .Lastmod.Unix) 604800 -}}
                {{- $priority := sub 1 (div $weeks 10.0 ) -}}
                {{- if ge .Sitemap.Priority $priority -}}
                    <priority>{{ .Sitemap.Priority }}</priority>
                {{- else -}}
                    <priority>{{ $priority }}</priority>
                {{- end -}}
            {{- end -}}

            {{- if .IsTranslated -}}
                {{- range .AllTranslations -}}
                    <xhtml:link
                        rel="alternate"
                        hreflang="{{ .Language.LanguageCode | default .Language.Lang }}"
                        href="{{ .Permalink }}"
                        />
                {{- end -}}
            {{- end -}}
        </url>
    {{- end -}}
</urlset>
